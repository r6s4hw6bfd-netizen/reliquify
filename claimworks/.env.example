# Neon Postgres connection string
DATABASE_URL=

# Resend API key (email, stage 3+)
RESEND_API_KEY=

# From address for client-facing reports/emails
REPORT_FROM_EMAIL=

# Public base URL of the app (used to build opt-in unsubscribe links)
APP_BASE_URL=

# Secret for signing magic-link + session tokens (any long random string)
AUTH_SECRET=
